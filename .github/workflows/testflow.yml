name: First_workflow
on: workflow_dispatch

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: print hello World
        run: echo "Hello World"
      - name: print bye
        run: echo "Good-bye"
