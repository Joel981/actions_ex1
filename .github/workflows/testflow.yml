name: First_workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout
      - name: node js
        uses: actions/setup-node
        with: 
          node-version: 18
      - name: install dependencies
        run: npm ci
      - name: test node
        run: npm test
