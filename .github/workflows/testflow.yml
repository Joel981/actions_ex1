name: First_workflow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        run: echo"get code ...."

  second_job:
    runs-on: ubuntu-latest
    steps:
      - name: Got code
        run: echo".... Received"